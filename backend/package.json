{
  "name": "opensen-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "typecheck": "tsc --noEmit",
    "build": "npx vercel build",
    "deploy": "npx vercel deploy"
  },
  "dependencies": {
    "hono": "^4.12.32",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  }
}
